sonar.projectKey=sustainable_finance_plataforma
sonar.projectName=Sustainable Finance Plataforma
sonar.projectVersion=1.0

# Fontes e testes
sonar.sources=src
sonar.tests=src
sonar.test.inclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx

# Exclusões (build, assets, tipos gerados, configs)
sonar.exclusions=\
  **/node_modules/**,\
  **/.next/**,\
  **/coverage/**,\
  **/public/**,\
  **/*.d.ts,\
  **/prisma/**,\
  **/cypress/**,\
  src/test/**

# Exclusões de cobertura
sonar.coverage.exclusions=\
  **/*.test.ts,\
  **/*.test.tsx,\
  **/*.spec.ts,\
  **/*.spec.tsx,\
  **/__tests__/**,\
  src/test/**,\
  src/lib/test/**,\
  src/types/**,\
  src/components/ui/**,\
  src/middleware.ts,\
  src/lib/data.ts,\
  src/data/seedContent.ts

# Exclusões de duplicação (CPD)
# Conteúdo estático bilíngue (PT/EN) e dados de seed: a estrutura PT/EN é
# naturalmente espelhada; não é lógica duplicada e não deve ser refatorada.
sonar.cpd.exclusions=\
  src/data/**,\
  src/lib/data.ts
  
# Relatório de cobertura gerado pelo Vitest (lcov)
sonar.typescript.lcov.reportPaths=coverage/lcov.info
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Servidor local (token passado em runtime pelo start.sh)
sonar.host.url=http://localhost:9000

# Encoding
sonar.sourceEncoding=UTF-8
